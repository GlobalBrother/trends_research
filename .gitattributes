# Ensure shell scripts always use LF line endings, even on Windows checkouts.
# Git Bash / WSL / Linux containers cannot parse `set -euo pipefail\r\n`.
*.sh        text eol=lf
Dockerfile* text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf

